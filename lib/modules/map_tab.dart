import 'package:flutter/material.dart';

class maps_tab extends StatefulWidget {
  const maps_tab({super.key});

  @override
  State<maps_tab> createState() => _maps_tabState();
}

class _maps_tabState extends State<maps_tab> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      backgroundColor: Colors.red,
      body: Column(
        children: [
      ]
    )
    );
  }
}
